{
  "name": "infra",
  "version": "1.0.0",
  "private": true,
  "description": "AWS CDK infrastructure for turnier-hub",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -w -p tsconfig.json",
    "cdk": "cdk",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy --all"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.251.0",
    "constructs": "^10.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.15",
    "aws-cdk": "^2.1119.0",
    "esbuild": "^0.27.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
