# vite-plugin-pwa declares vite only through ^7; this repo uses Vite 8. npm would
# fail with ERESOLVE on a clean install without this. Revisit when upstream adds
# || ^8.0.0 to peerDependencies: https://github.com/vite-pwa/vite-plugin-pwa/issues/923
legacy-peer-deps=true
