<script setup lang="ts">
/**
 * Root for tournament (and other routed) stories: one `<router-view />` matches the
 * in-memory preview router, mirroring `App.vue` so nested layouts are not mounted twice.
 */
</script>

<template>
  <router-view />
</template>
