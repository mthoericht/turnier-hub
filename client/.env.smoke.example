# Cloud smoke-test SPA build (generated copy — see `npm run smoke:outputs -- --write-env`)
#
# Vite loads `.env.production.local` automatically for `npm run build -w client`.
# Prefer the helper scripts instead of hand-editing:
#   npm run smoke:outputs -- --write-env
#   npm run smoke:build-spa

VITE_AUTH_PROVIDER="cognito"
VITE_COGNITO_USER_POOL_ID="eu-central-1_XXXXXXXXX"
VITE_COGNITO_CLIENT_ID="xxxxxxxxxxxxxxxxxxxxxxxxxx"
VITE_API_BASE_URL="https://d1234567890.cloudfront.net"
